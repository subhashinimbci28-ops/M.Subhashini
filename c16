#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
//Complete the following function.


void calculate_the_maximum(int n, int k) {
    int max_and = 0;
    int max_or = 0;
    int max_xor = 0;

    // Loop through all unique pairs (i, j) where 1 <= i < j <= n
    for (int i = 1; i < n; i++) {
        for (int j = i + 1; j <= n; j++) {
            
            // Calculate Bitwise AND
            int current_and = i & j;
            if (current_and > max_and && current_and < k) {
                max_and = current_and;
            }

            // Calculate Bitwise OR
            int current_or = i | j;
            if (current_or > max_or && current_or < k) {
                max_or = current_or;
            }

            // Calculate Bitwise XOR
            int current_xor = i ^ j;
            if (current_xor > max_xor && current_xor < k) {
                max_xor = current_xor;
            }
        }
    }

    // Print the maximum values found on separate lines
    printf("%d\n%d\n%d\n", max_and, max_or, max_xor);
}

int main() {
    int n, k;
  
    scanf("%d %d", &n, &k);
    calculate_the_maximum(n, k);
 
    return 0;
}
